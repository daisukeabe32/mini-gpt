# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
*.egg-info/

# ── Virtual environments ──────────────────────────────────────────────────────
venv/
.env/
.venv/
env/
conda_env/

# ── Editors / OS ──────────────────────────────────────────────────────────────
.vscode/
.DS_Store

# ── Large binary artifacts ────────────────────────────────────────────────────
# PyTorch tensors and pickles (checkpoints, tokenized ids)
*.pt
*.pkl

# ── Data ──────────────────────────────────────────────────────────────────────
# Pre-tokenized datasets (large .pt + .json; regenerate via scripts/tokenize_corpus.py)
data/tokenized/

# Large raw corpora downloaded externally (not committed)
data/tinystories.txt

# data/shakespeare.txt IS tracked (1.1 MB reference corpus)

# ── Training artifacts ────────────────────────────────────────────────────────
# Model checkpoints (large binaries; tracked via W&B)
checkpoints/

# W&B local run cache (synced to cloud)
wandb/

# Generated figures from analysis scripts (regenerate via scripts/analyze_attention.py)
figs/

# ── Personal runbooks / operational notes ─────────────────────────────────────
# Account-specific Kaggle procedures; not part of the codebase
KAGGLE_RUNBOOK.md
